import React from "react";
import { View, Text } from "react-native";

const UmamiMenuInfo = ({}) => {
  return (
    <View>
      <Text> Menu: Info</Text>
    </View>
  );
};

export default UmamiMenuInfo;
