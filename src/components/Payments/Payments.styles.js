import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  paymentMethodViewContainer: {
    marginTop: 20,
  },
  paymentMethodViewOptions: {
    alignItems: "center",
    flexDirection: "row",
    justifyContent: "space-between",
    marginTop: 10,
    paddingBottom: 5,
  },
});

export const stylesRNEComponents = {
  paymentMethodTitle: {
    textAlign: "center",
  },
};

export default styles;
