import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  deliveryInfoTitle: {
    textAlign: "center",
  },
  deliveryInfoViewContainer: {
    marginTop: 20,
  },
  deliveryInfoViewOptions: {
    alignItems: "center",
    flexDirection: "row",
    justifyContent: "space-between",
    marginTop: 10,
    paddingBottom: 5,
  },
});

export default styles;
